#!/bin/bash

filename=${1:-london_home}
publicIp=$(curl icanhazip.com)
dateUptoHour=$(date -Ih)
FOLDER="$HOME/shared_folders/ldn_desktop_share/cronlog"
mkdir -p $FOLDER
echo "$dateUptoHour $publicIp" > "$FOLDER/${filename}-${dateUptoHour}.txt"
