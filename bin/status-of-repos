#!/bin/bash
# Pass Find parameters if required

FIND_PARAMS="$@"

#check if git repos are changed/behind
for path in $(find $PWD -iname .git $@)
do
  REPOROOT=${path/%.git/}
  pushd "$REPOROOT" > /dev/null
    GIT_STATUS=$(git -c color.ui=always status -sb)
    if grep behind <<< "$GIT_STATUS" > /dev/null ; then
        echo "$REPOROOT"
        echo "$GIT_STATUS"
    fi
  popd > /dev/null
  # git diff --stat
done
