#!/bin/bash
# FOLDER_NAME=$1
# mkdir -p ~/tars/$FOLDER_NAME/$(date -I)
# cd ~/tars/$FOLDER_NAME/$(date -I)
# tar cvf - ~/$FOLDER_NAME | split --verbose -b 2G - ${FOLDER_NAME}.tgz.part_ -d

aws s3 sync --storage-class GLACIER ~/aws_s3/ s3://onlinebackup/pictures/
