#!/bin/bash
source ~/devsetup/bin/dev-helpers/helper-functions
source ~/devsetup/bin/proxy
attach_to_tmux home home
alias t='attach_to_tmux home home'

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
